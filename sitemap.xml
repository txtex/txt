---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {%- for post in site.posts -%}
    {%- unless post.sitemap == false -%}
    <url>
      <loc>{{ site.url | append: site.baseurl }}{{ post.url | xml_escape }}</loc>
      {%- if post.last_modified_at -%}
        <lastmod>{{ post.last_modified_at | date_to_xmlschema }}</lastmod>
      {%- elsif post.date -%}
        <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
      {%- endif -%}
    </url>
    {%- endunless -%}
  {%- endfor -%}
  {%- for page in site.html_pages -%}
    {%- unless page.sitemap == false or page.url contains "404" or page.url contains ".xml" or page.url contains ".txt" -%}
    <url>
      <loc>{{ site.url | append: site.baseurl }}{{ page.url | xml_escape }}</loc>
      {%- if page.last_modified_at -%}
        <lastmod>{{ page.last_modified_at | date_to_xmlschema }}</lastmod>
      {%- endif -%}
    </url>
    {%- endunless -%}
  {%- endfor -%}
</urlset>